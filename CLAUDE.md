# CLAUDE.md — Template Base de Projetos

> Arquivo de contexto para o Claude Code (VSCode).
> Copie para a raiz de cada projeto novo e ajuste a seção "Projeto Atual".


---
## Protocolo de início de sessão (OBRIGATÓRIO)
Antes de qualquer tarefa — sem exceção — leia nesta ordem:

STATUS.md → estado atual do projeto, o que está feito, o que está em andamento
DECISIONS.md → decisões arquiteturais e de domínio já tomadas
Este CLAUDE.md → stack, padrões e regras

Se qualquer um desses arquivos não existir, avise imediatamente antes de prosseguir.
Ao fim de cada sessão, atualize o STATUS.md com:

O que foi concluído nesta sessão
O que ficou em andamento
Qualquer nova decisão (registre também no DECISIONS.md)
Novas armadilhas encontradas

---

## Projeto Atual

```
Nome:        agenda-protese-digital
Objetivo:    Sistema digital de gerenciamento do fluxo de próteses dentárias
             da Secretaria de Saúde de Atibaia/SP — da triagem ao pós-entrega.
Status:      Funcional — previsão de tempo para próxima consulta visível ao agendar retorno
Próxima tarefa: A definir
```

---

## Skills disponíveis
- Neon Database: C:\Users\Alcatoo\claude-skills\neon-postgres\SKILL.md
- Auth: C:\Users\Alcatoo\claude-skills\auth-flexivel\SKILL.md
- Postgres: C:\Users\Alcatoo\claude-skills\postgres-standards\SKILL.md
- UX/UI: C:\Users\Alcatoo\claude-skills\ux-ui-base\SKILL.md
- Shadcn Studio: C:\Users\Alcatoo\claude-skills\shadcn-studio\SKILL.md

---


---

## Perfil do Desenvolvedor

- **Cargo**: Técnico administrativo — Secretaria de Saúde, Atibaia/SP
- **Nível JS/TS**: Iniciante avançado (domina conceitos, consulta referências)
- **Abordagem**: Pragmática, orientada a problemas reais, autodidática
- **Idioma**: Português em comunicação e comentários; inglês em variáveis, funções, classes e nomes de arquivos

---

## Stack Padrão

| Camada | Tecnologia | Versão mínima |
|--------|-----------|---------------|
| Runtime | Node.js | >= 24.0.0 |
| Gerenciador de pacotes | pnpm | >= 10.0.0 |
| Framework | SvelteKit | >= 2.5.0 |
| UI reativa | Svelte (runes) | >= 5.5.0 |
| CSS | TailwindCSS | 4.x (sem tailwind.config.js) |
| Componentes | shadcn-svelte | latest |
| ORM | Drizzle ORM | latest |
| Banco | Neon PostgreSQL | 17 |
| Deploy | Vercel | — |
| Gráficos | Apache eCharts |

---

## Ambiente e Setup Inicial

### .npmrc (raiz do projeto)
```
engine-strict=true
shamefully-hoist=false
```

### package.json — engines obrigatório
```json
"engines": {
  "node": ">=24.0.0",
  "pnpm": ">=10.0.0"
}
```

### .gitignore padrão
```
.env
.env.local
.env*.local
.vercel
.svelte-kit/
node_modules/
dist/
```

### Comandos de início
```bash
pnpm install        # instalar dependências
pnpm dev            # desenvolvimento
pnpm build          # build de produção
pnpm db:generate    # gerar migrations Drizzle
pnpm db:push        # aplicar schema no banco
```

---

## Estrutura de Pastas

```
src/
├── lib/
│   ├── components/         ← componentes de domínio (ex: AgendamentoCard.svelte)
│   │   └── ui/             ← componentes genéricos (Button, Input, Modal)
│   ├── server/             ← código exclusivo do servidor
│   │   └── db/
│   │       ├── schema.ts   ← definição de tabelas Drizzle
│   │       └── relations.ts
│   └── utils/              ← funções puras reutilizáveis
├── routes/                 ← páginas e endpoints SvelteKit
└── app.css                 ← tokens Tailwind v4 (@theme, @layer)
```

---

## Convenções de Código

### TypeScript
- `strict: true` sempre ativo
- `noUncheckedIndexedAccess: true` — array[i] pode ser undefined
- Props tipadas explicitamente; nunca `any` sem justificativa

### Svelte 5 — runes obrigatório
```svelte
<script lang="ts">
  // ✅ correto — variáveis em inglês, comentários em português
  let count = $state(0)
  let doubled = $derived(count * 2)
  let { onSave }: { onSave: () => void } = $props()

  // ❌ nunca usar sintaxe Svelte 4
  // let count = 0
  // $: doubled = count * 2
</script>
```

### Nomes de arquivos e identificadores
- Componentes Svelte: `kebab-case.svelte`
- Arquivos TypeScript: `kebab-case.ts`
- Variáveis, funções, classes, props: **inglês**
- Componentes genéricos: `src/lib/components/ui/`
- Componentes de domínio: `src/lib/components/`

### Comentários
- Sempre em **português**
- Explicar o "porquê", não o "o quê"

---

## Banco de Dados — Regras Rápidas

> Regras completas na skill `postgres-standards`.

- PK: `id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY`
- Nunca `SERIAL`, `UUID` como PK padrão, `TIMESTAMP` sem timezone
- Timestamps: `TIMESTAMPTZ NOT NULL DEFAULT NOW()`
- Booleanos: prefixo afirmativo (`is_active`, `has_discount`)
- Soft delete: `deleted_at TIMESTAMPTZ` (não `is_deleted`)
- FKs sempre têm índice manual correspondente
- Migrations: apenas via `drizzle-kit generate` — nunca editar à mão

---

## Git — Conventional Commits

Formato: `tipo: descrição curta em português`

```
feat: adiciona filtro por unidade de saúde
fix: corrige serialização de data no agendamento
chore: atualiza dependências
refactor: extrai lógica de cálculo gestacional
docs: atualiza DECISIONS.md com escolha de auth
```

Tipos válidos: `feat` · `fix` · `chore` · `refactor` · `docs` · `test` · `style`

---

## Autenticação

> Regras completas na skill `auth-flexivel`.

- Provider padrão: **Neon Auth**
- Fallback: Google OAuth fechado (usuários pré-cadastrados)
- Troca via `AUTH_PROVIDER=neon|google` no `.env` — sem alterar código

---

## UX/UI

> Regras completas na skill `ux-ui-base`.

- Cores: OKLCH (sem HSL)
- Contraste mínimo: WCAG AA (4.5:1)
- TailwindCSS 4.x via `@theme` no CSS — sem `tailwind.config.js`
- Público: servidores públicos sob pressão → eficiência > estética

---

## Estilo de Resposta Esperado

1. **Analogia** — ilustra o conceito
2. **Contexto** — apresenta o problema
3. **Solução** — abordagem e ferramentas
4. **Exemplo** — implementação prática completa
5. **Próximos passos** — como evoluir

- Código sempre completo (nunca fragmentos)
- Princípio de Pareto: 80/20 em funcionalidades, exemplos e explicações
- Incluir comandos de instalação quando aplicável
- Nunca usar `npm` — sempre `pnpm`

---

## MCPs Disponíveis

| MCP | Usar para |
|-----|-----------|
| Neon Database | queries diretas, inspecionar schema, debug SQL |
| GitHub | commits, branches, PRs, issues |
| Context7 | documentação live de libs (Svelte, Drizzle, SvelteKit) |
| Shadcn Studio | gerar/refinar componentes shadcn-svelte |

---

## Pitfalls Conhecidos

- `Date` no `google.script.run` → serializar como string ISO antes de passar
- TailwindCSS v4: configuração via `@theme` no CSS, não em `tailwind.config.js`
- Drizzle `timestamp`: sempre `{ withTimezone: true }`
- Soft delete: filtrar sempre com `.where(isNull(tabela.deletedAt))`
- Neon + OAuth: usar `127.0.0.1` no redirect URI em dev, não `localhost`
- pnpm com `engine-strict=true`: versão local do Node precisa bater com `engines`
- **`pnpm db:push` PROIBIDO neste projeto** — o banco tem tabelas legadas que o Drizzle desconhece; o push propõe `DROP SCHEMA protese` com 26 tabelas. Para mudanças de schema: usar `ALTER TABLE` via MCP Neon (ver DECISIONS.md 2026-04-01)
- **Comparação de `time` com string**: campos `time` do PostgreSQL chegam como `"HH:MM:SS"`, mas `<input type="time">` retorna `"HH:MM"` — nunca comparar diretamente. Usar `timeToMinutes()` de `src/lib/slots.ts`

### Better Auth (Neon Auth)

- **Sem schema `public`** neste banco Neon → criar schema explícito (`auth`, `protese`, etc.) antes de qualquer `CREATE TABLE`
- **`basePath` obrigatório**: se a rota handler está em `/auth/[...all]`, configurar `basePath: '/auth'` no servidor (`betterAuth({...})`) E no cliente (`createAuthClient({...})`) — o padrão do Better Auth é `/api/auth`
- **Login social é POST, não GET**: usar `authClient.signIn.social({ provider: 'google', callbackURL: '/destino' })` — link direto para `/auth/sign-in/social/google` retorna 404
- **`@auth/sveltekit` v1**: retorna `{ handle, signIn, signOut }` — sem `handlers`. Sessão via `event.locals.auth()`, não `event.locals.session`
- **Callback URI em dev**: `http://127.0.0.1:5173/auth/callback/google` (não `localhost`)
